name: ai-service
on:
  pull_request:
    paths: ["ai-service/**", ".github/workflows/ai-service.yml"]
  push:
    branches: [main]
    paths: ["ai-service/**"]

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ai-service
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install -e ".[dev]"
      - run: ruff check .
      - run: mypy app
      - run: pytest -q
