{
  "name": "quoteassist-outlook-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "QuoteAssist Outlook add-in (Office.js). Phase 2 deliverable — stub scaffold.",
  "type": "module",
  "scripts": {
    "lint": "echo \"(eslint is wired up in Phase 2)\" && exit 0",
    "typecheck": "tsc --noEmit",
    "build": "echo \"(vite build is wired up in Phase 2)\" && exit 0"
  },
  "devDependencies": {
    "typescript": "~5.6.0"
  }
}
